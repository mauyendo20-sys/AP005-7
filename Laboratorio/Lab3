from machine import Pin, PWM, ADC
import time

led = PWM(Pin(5))
led.freq(1000)  


pot = ADC(Pin(4))
pot.atten(ADC.ATTN_11DB) 

while True:

    valor = pot.read_u16() 
    
    led.duty_u16(valor)
    
    time.sleep(0.05)
